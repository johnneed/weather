'use strict';

/**
 * @ngdoc service
 * @name weatherApp.weatherData
 * @description
 * # weatherData
 * Service in the weatherApp.
 */
angular.module('weatherApp')
  .service('weatherData', function weatherData() {
    // AngularJS will instantiate a singleton by calling "new" on this function
  });
